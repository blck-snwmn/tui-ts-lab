{
  "name": "tui-ts-lab",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "fmt": "oxfmt apps/ --write",
    "fmt:check": "oxfmt apps/ --check",
    "lint": "oxlint --deny-warnings .",
    "lint:fix": "oxlint --deny-warnings . --fix"
  },
  "devDependencies": {
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "oxlint-tsgolint": "^0.22.1"
  }
}
